import React from 'react';

const Cart = ({cart}) => {
    console.log(cart)
    return (
        <div>
            <h1>hello cart container</h1>
        </div>
    );
};

export default Cart;